# Server-side only. Locally read by server/dev-server.js via `node --env-file`;
# in production it's the same variable the Vercel Function (api/transcribe.js)
# reads from process.env, set directly in the Vercel dashboard.
# Never prefixed with VITE_, never bundled to the client, never committed.
GROQ_API_KEY=
